declare class Rubiks {
    private camera;
    private scene;
    private renderer;
    private controls;
    private loop;
    constructor(container: Element);
    private render;
}
export default Rubiks;
